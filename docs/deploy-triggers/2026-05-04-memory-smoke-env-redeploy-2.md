# Production redeploy trigger

Triggering a fresh production deployment after enabling `DSG_ALLOW_DEV_AUTH_HEADERS=true` for the governed memory API smoke test.

Claim boundary: this is a dev-route smoke proof trigger only, not a production auth/RBAC claim.
